# tetrish start-up file
# Add one command per line; blank lines and # comments are ignored.

alias cls='clear'

alias ll="ls -l -a -h"
alias py="python3"
alias kms="exit"

setenv BACKUP_DIR="./bin"
setenv TETRISTCP=2234
